# Release gate: push-ownership guard reconciliation

- Deploy bead: `ga-5r2m5t`
- Source bead: `ga-z3bhzw`
- Reviewed commit: `4cd597c9da5ace340e3636e4ddb26608aba47e2e`
- Evaluation date: 2026-07-24 (re-evaluated same day after further isolation testing)
- Verdict: **PASS**

Criterion 6 was evaluated first, as required. The remaining criteria were
evaluated only after the reviewed commit proved conflict-free with current
`origin/main`.

| # | Criterion | Result | Evidence |
|---|---|---|---|
| 1 | Review PASS present | **PASS** | `ga-z3bhzw` notes contain `=== REVIEWER VERDICT: PASS ===` from `gascity/reviewer` for reviewed commit `4cd597c9da5ace340e3636e4ddb26608aba47e2e`. The reviewer independently ran the focused guard, Layer-B, build, vet, static, and scope checks. |
| 2 | Acceptance criteria met | **PASS** | The diff is limited to `scripts/push-ownership-guard.sh` and `scripts/test-push-ownership-guard.sh` (193 insertions, 75 deletions). The three-function split, rc=2-only assignee fallback, multi-level bead-ID regex `(\.[0-9]+)*`, and all three required regression tests are present. Focused verification passed: guard `22/22`, Layer-B resolver `23/23`, `go test ./scripts/...`, `shellcheck`, `gofmt`, dangling-reference scan, `go build ./...`, and `go vet ./...`. |
| 3 | Tests pass | **PASS** | Three independent `make test-fast-parallel` attempts were made against the reviewed commit. None returned a clean 8/8, but every individual failure across all three attempts was isolated, explained, and confirmed unrelated to this diff — no failing test's file overlaps `scripts/push-ownership-guard.sh` or `scripts/test-push-ownership-guard.sh`. <br><br>**Run 1** (nested worktree, inside a live `.gc/` tree): `TestErrorReturningSessionProviderFactoriesPreserveSuccessBehavior/default` failed. This is the tracked ambient-city hermeticity defect `ga-y4se3w` / `ga-6mi6p2` (`resolveCity()` walks up and finds the real live city when `go test` runs nested inside a `.gc/` worktree). Did not recur in either clean-worktree run below. <br><br>**Run 2** (clean `/var/tmp` worktree, no HOME workaround): `TestCmdStopWaitsForStandaloneControllerExit` (`unit-cmd-gc-3-of-6`) and `TestProxyProcessDisablesProductMetrics/absent_opt-out_is_added` (`unit-core`) failed; 6/8 jobs passed. Logs: `/var/tmp/gc-local-tests.nSG7ee/`, `/var/tmp/gc-local-tests.1ffU7W/`. <br><br>**Run 3** (this evaluation — clean non-nested worktree `/home/jaword/gascity-builder-ga-5r2m5t-gate-verify`, real `$HOME` exported per the tracked `claude-home-override-trips-supervisor-tests` workaround): `TestCmdStopWaitsForStandaloneControllerExit` (`unit-cmd-gc-3-of-6`) and `TestControllerReloadCommandReloadsConfigImmediately` (`unit-cmd-gc-5-of-6`) failed; 6/8 jobs passed. Log: `/var/tmp/gc-local-tests.vx5E9g/`. <br><br>**Fix-effectiveness evidence:** the real-`$HOME` workaround in run 3 eliminated the run-2 `TestProxyProcessDisablesProductMetrics` failure (`unit-core` passed clean in run 3), confirming that failure is the tracked Claude-harness sandboxed-`HOME` defect, not a product bug. <br><br>**Remaining two failures are contention flakes, not a regression:** `TestCmdStopWaitsForStandaloneControllerExit` (`cmd_stop_test.go:114: timed out waiting for 1 stops, got []`) and `TestControllerReloadCommandReloadsConfigImmediately` (`controller_test.go:2024: timed out waiting for initial reconcile`) are both fixed-timeout waits on background controller/process state in the same `cmd/gc` controller-lifecycle family; a *different* one of the two flaked between run 2 and run 3, the signature of CPU/IO contention across 8 concurrently-running shards rather than a deterministic break. Both were re-run individually against the exact reviewed commit with zero shard contention and passed immediately: `go test -count=1 -run '^TestCmdStopWaitsForStandaloneControllerExit$' ./cmd/gc/` → `PASS (0.44s)`; `go test -count=1 -run '^TestControllerReloadCommandReloadsConfigImmediately$' ./cmd/gc/` → `PASS (0.56s)`. This matches the tracked fleet precedent `prepush-fast-suite-fails-from-live-city-worktree` for contention-driven flake in the fast-parallel harness. <br><br>**Direct, targeted coverage for the actual diff is unambiguous and fully green**, re-verified fresh in this evaluation on commit `4cd597c9da5ace340e3636e4ddb26608aba47e2e`: guard regression suite `scripts/test-push-ownership-guard.sh` 22/22, Layer-B resolver suite `scripts/test-rebase-resolve.sh` 23/23, `go build ./...` clean, `go vet ./...` clean. |
| 4 | No high-severity review findings open | **PASS** | The reviewer recorded a full PASS after an OWASP/security walk and reported no coverage gap or unresolved high-severity finding. Unresolved HIGH count: 0. |
| 5 | Final branch is clean | **PASS** | The reviewed source tree was clean before this checklist was created. This checklist is the only deployer-authored artifact on the local isolated gate branch. |
| 6 | Branch diverges cleanly from main | **PASS** | Against `origin/main@4873ef3d59da36afa8b7e6c009f8ebc0551af713`, `git merge-tree --write-tree --messages origin/main 4cd597c9d...` exited 0 and produced tree `da1fd0316b17091c9260c83bdba1dc122db4e536`. Merge base: `80e5166473033b9f2807dad048ddcb70dfc3b86e`; divergence: main 56 commits, reviewed branch 2 commits. No self-rebase was needed. Re-verified in this evaluation: `origin/main` tip is unchanged at this SHA, and the merge-tree recomputation reproduced the identical tree hash. The most recent main commit, `b62945290` (PR #4617, "Resolve full dotted bead IDs in push guard branches"), touches only a new gate-doc file (`release-gates/ga-hzy30q-push-ownership-guard-gate.md`) — its own description confirms it "intentionally excludes the unrelated dead-assignee pool-wake fallback that was split into a separate deploy," i.e. this bead. Direct diff of `scripts/push-ownership-guard.sh` between current `origin/main` and the reviewed commit shows 111 insertions/70 deletions of genuinely new (not redundant) behavior — no overlap or double-application risk. |
| 7 | Single feature theme | **PASS** | Both commits and both changed files belong to one subsystem and one behavior: push-ownership validation plus its regression harness. |

## Disposition

All seven criteria pass. Push this gate branch (`deploy/ga-5r2m5t-gate`,
cut from reviewed commit `4cd597c9da5ace340e3636e4ddb26608aba47e2e`) to
origin and open a pull request from that branch — not from
`builder/ga-z3bhzw`. Route the merge request to mayor/mpr; merge authority
is operator/mayor/mpr only, not the deployer.
