feat(reconciler): trace the quarantine wake refusal and record the WD.10b deltas

Delta-8 arm 3, taken here because it is the one arm of the three that needs no
stand-down of its own: legacy drops a wake target inside a live quarantine or
hold window with NO trace record at all, so the parity join sees a wake that
never happened and nothing saying why. detectWakeOrSleep now records
detector_wake_blocked with the blocker and enqueues nothing. It cannot
double-act - legacy already skips the row and the keyed admission chain blocks
it again at the handler - so the refusal is a non-action on both sides. RED
proven non-vacuous by reverting only the blocker hunk: the quarantined row came
back as an ordinary routed start candidate.

DETECTOR.md:

  - §2 declares the routed-work read as LANDED and records the discharged WE
    ledger entry symbol by symbol, plus where invalidation moved and where
    WD.16's cadence floor now rides;
  - §2's degradation rule drops the legacy-fallback crutch and states the
    census-owed rule for both overflow and unhandled allocations;
  - WD.1 delta 7 records the ready-demand scan as ABSORBED rather than
    inventoried;
  - WD.10a delta 8 is updated with which of its three handoffs WD.10b took;
  - a new WD.10b delta section records: the FILL arm carrying no session key and
    therefore no admission source; its yield being an ALLOCATION seam against a
    different legacy engine than the start seam; open-member counting answering
    from the planner's one spelling; the :583/:614 family resolved by
    construction on the keyed path with the residual refill belonging to
    legacy's ungated FLOOR path until WE; and exactly which delta-8 work is
    deferred, why moving the churn/wake-failure accounting without a
    forward-pass stand-down would double-count wake_attempts, and that the
    :4873/:6927 anchors stay pointed at legacy until that arm lands.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
